const BorderButton = ({ style }) => (
    <button
        type="button"
        className={`w-[233px] h-[64px] rounded-[12px] border-primary border-[1px] bg-[#FBFBFB] text-primary cursor-pointer ${style}`}>
        View More
    </button>
);
export default BorderButton;
